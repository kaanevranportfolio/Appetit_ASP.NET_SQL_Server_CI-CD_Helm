apiVersion: v1
kind: Service
metadata:
  name: {{ include "restaurantmenuapi.fullname" . }}
  labels:
    {{- include "restaurantmenuapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "restaurantmenuapi.selectorLabels" . | nindent 4 }}
